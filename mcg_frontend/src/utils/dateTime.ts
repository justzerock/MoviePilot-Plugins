const DEFAULT_TIME_ZONE = 'Asia/Shanghai'
const toDate = (value: string | number | Date) => value instanceof Date ? value : new Date(typeof value === 'number' && value < 1e12 ? value * 1000 : value)
export function formatDateTime(value: string | number | Date, timeZone = DEFAULT_TIME_ZONE): string { const date = toDate(value); if (Number.isNaN(date.getTime())) return '未知时间'; return new Intl.DateTimeFormat('zh-CN', { timeZone, year: 'numeric', month: '2-digit', day: '2-digit', hour: '2-digit', minute: '2-digit', hour12: false }).format(date).replaceAll('/', '-') }
export function formatTimelineTime(value: string | number | Date, timeZone = DEFAULT_TIME_ZONE): string { const date = toDate(value); if (Number.isNaN(date.getTime())) return '未知时间'; const now = new Date(); const day = (item: Date) => new Intl.DateTimeFormat('en-CA', { timeZone, year: 'numeric', month: '2-digit', day: '2-digit' }).format(item); const time = new Intl.DateTimeFormat('zh-CN', { timeZone, hour: '2-digit', minute: '2-digit', hour12: false }).format(date); if (day(date) === day(now)) return `今天 ${time}`; if (day(date) === day(new Date(now.getTime() - 86400000))) return `昨天 ${time}`; return `${new Intl.DateTimeFormat('zh-CN', { timeZone, month: 'numeric', day: 'numeric' }).format(date)} ${time}` }
